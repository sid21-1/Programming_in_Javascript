const array=[23,1,4,5,2,34]
sortArray(array)

function sortArray(arr){
    arr.sort();
}

console.log(array);

//Remember the array sorts the numbers in the form of a string so you have to convert it into number